ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [com.codex.ApplicationTestEnvironmentKt.moduleTest]
  }

  properties {
    location = configurations/application.properties
    location = ${?properties}
  }
}
